import SwiftUI

/// The website's palette, so the phone doesn't feel like a different product.
enum Theme {
    static let background = Color(red: 0.055, green: 0.059, blue: 0.067)
    static let surface = Color(red: 0.086, green: 0.094, blue: 0.106)
    static let line = Color(red: 0.165, green: 0.176, blue: 0.192)
    static let text = Color(red: 0.925, green: 0.929, blue: 0.933)
    static let textMuted = Color(red: 0.608, green: 0.631, blue: 0.651)
    static let textFaint = Color(red: 0.431, green: 0.455, blue: 0.478)
    static let accent = Color(red: 0.357, green: 0.659, blue: 0.961)
    static let good = Color(red: 0.290, green: 0.765, blue: 0.541)
    static let bad = Color(red: 0.898, green: 0.478, blue: 0.478)
}
