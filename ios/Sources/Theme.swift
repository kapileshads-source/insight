import SwiftUI

/// The website's palette, so the phone doesn't feel like a different product.
enum Theme {
    static let background = Color(red: 0.075, green: 0.075, blue: 0.086)
    static let surface = Color(red: 0.106, green: 0.106, blue: 0.125)
    static let line = Color(red: 0.149, green: 0.149, blue: 0.173)
    static let text = Color(red: 0.929, green: 0.929, blue: 0.941)
    static let textMuted = Color(red: 0.612, green: 0.612, blue: 0.659)
    static let textFaint = Color(red: 0.478, green: 0.478, blue: 0.529)
    static let accent = Color(red: 0.122, green: 0.663, blue: 0.478)
    static let good = Color(red: 0.247, green: 0.749, blue: 0.561)
    static let bad = Color(red: 0.886, green: 0.337, blue: 0.247)
}
